// Define the Header component as a functional component.
const Header = () => {
    return (
        <div className="mb-10">
            {/* Display a heading with a specific style */}
            <h1 className="font-bold text-center mt-10 text-2xl">Course Registration</h1>
        </div>
    );
};

// Export the Header component as the default export.
export default Header;
